apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rag-v2-route
  namespace: default
spec:
  parentRefs:
  - group: ""
    kind: Service
    name: rag
    port: 80
  rules:
  - matches:
    - path:
        type: Prefix
        value: /
    backednRef:
    - name: rag-v2
      kind: Service
      port: 80
      weight: 100
